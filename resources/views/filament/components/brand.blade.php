<div class="flex items-center gap-3">
    <img src="{{ asset('comm-health-icon.png') }}" alt="{{ config('app.name') }}" class="h-10">
    <span class="text-xl font-semibold text-gray-800 dark:text-gray-200">
        {{ config('app.name') }}
    </span>
</div>
